# Этот файл подробно описывает процесс формирование анализа 

## Формирование анализа

**Где:** Бэкенд  

**Триггер:** Событие "Формирование контекста" 

**Результат:** Сформированный аналитический набор данных для страницы "Анализ"

---

## Общий pipeline формирования анализа

1. Получение данных из БД:
   - Данные планирования (`Название мультипроекта`, `Горизонт планирования`)
   - Реестр проектов (`Название проекта`, `Статус`, `Дата начала`, `Дата окончания`, `Трудоемкость: аналитики`, `Трудоемкость: разработчики`, `Трудоемкость: тестировщики`, `Ограничения прокта`, `Текущие отклонения`, `Описание проекта`)
   - Зависимости проектов (`Основной проект`, `Зависимый проект`)
   - Портфельные ограничения (`Лимит: аналитики`, `Лимит: разработчики`, `Лимит: тестировщики`, `Критический срок`)

2. Валидация и нормализация данных

3. Последовательное формирование аналитических блоков:
   1. Граф зависимостей проектов
   2. Список выявленных рисков
   3. Анализ реализуемости портфеля по срокам
   4. Анализ загрузки и дефицита ресурсов по ролям
   5. Анализ текущих отклонений
   6. ИИ-объяснения
   7. ИИ-рекомендации

4. Сохранение результатов

---

### 1. Граф зависимостей проектов

**Определение:** Граф, отображающий проекты как узлы и зависимости как направленные ребра. Атрибуты узлов: `Статус проекта` (Завершено, В работе, Не начато).

**Входные данные:**
(поступают из БД, ранее были введены пользователем через интерфейс)
- `Реестр проектов`
- `Зависимости проектов`

Формат:
```json
projects: [
    { 
        "name": name, 
        "status": status
    }
]
dependencies: [
    { 
        "mainProject": mainProject, 
        "dependentProject": dependentProject 
    }
]
```

**Алгоритм:**
1. Создание узлов 
```json
nodes = [
  {
    "id": project.id, 
    "name": project.name,
    "status": project.status
  }
]
```
2. Создание ребер
```json
edges = [
  {
    "source": dep.mainProject,
    "target": dep.dependentProject
  }
]
```
3. Определение статуса узлов
    - Завершено → зеленый
    - В работе → синий
    - Не начато → серый

**Формат результата:**
```json
{
  "nodes": [
    { 
        "id": "id123", 
        "name": "P1", 
        "status": "В работе"
    }
  ],
  "edges": [
    { 
        "source": "P1", 
        "target": "P2" 
    }
  ]
}
```

**Особенности:**
- запрещены циклы
- обязательная проверка существования проектов

---

### 2. Список выявленных рисков

**Определение:** Список рисков, выявленных на основе анализа данных

**Входные данные:**
- Реестр проектов (`Название проекта`, `Статус`, `Дата начала`, `Дата окончания`, `Трудоемкость: аналитики`, `Трудоемкость: разработчики`, `Трудоемкость: тестировщики`, `Ограничения прокта`, `Текущие отклонения`, `Описание проекта`)
- Зависимости проектов (`Основной проект`, `Зависимый проект`)
- Портфельные ограничения (`Лимит: аналитики`, `Лимит: разработчики`, `Лимит: тестировщики`, `Критический срок`)
- Данные планирования (`Название мультипроекта`, `Горизонт планирования`)

---

**Используемые поля:**
```json
{
  "planningData": {
    "portfolioName": "Развитие ИС компании",
    "planningHorizon": "31.07.2026"
  },
  "projects": [
    {
      "id": "P1",
      "name": "Модуль авторизации",
      "status": "В работе",
      "startDate": "01.03.2026",
      "endDate": "30.04.2026",
      "workload": {
        "analysts": 120,
        "developers": 300,
        "testers": 80
      },
      "constraints": "Нельзя переносить релиз",
      "deviations": "Отставание на 10 дней",
      "description": "Разработка модуля авторизации"
    }
  ],
  "dependencies": [
    {
      "mainProject": "P2",
      "dependentProject": "P1",
      "description": "Проект P2 зависит от проекта P1"
    }
  ],
  "portfolioConstraints": {
    "analystsLimit": 500,
    "developersLimit": 900,
    "testersLimit": 300,
    "criticalDeadline": "31.07.2026"
  }
}
```
---

**Валидация данных:**
1. В `Реестре проектов` есть хотя бы один проект.
2. У каждого проекта заполнены:
    - название;
    - статус;
    - дата начала;
    - дата окончания;
    - трудоемкость по ролям.
3. Все даты приводятся к единому формату.
4. Дата начала проекта не позже даты окончания.
5. Все зависимости ссылаются на существующие проекты.
6. В зависимостях нет циклов.
7. Лимиты ресурсов не являются отрицательными.
8. Горизонт планирования и критический срок заполнены.

Если данные не проходят проверку, риск не формируется, а процесс возвращает ошибку валидации.

---

#### Формирование аналитических проблем

**Проблема** — это внутренняя аналитическая доменная модель, используемая backend-слоем для промежуточного представления выявленного нарушения. Она не является публичной сущностью интерфейса и не возвращается на фронтенд напрямую.

Пример: 
```json
{
  "problemId": "problem-001",
  "type": "deadline_violation",
  "source": "timeline_analysis",
  "mainProject": "P3",
  "dependentProject": ["P1", "P2"],
  "reason": "Дата окончания проекта выходит за горизонт планирования",
  "facts": {
    "projectEndDate": "20.08.2026",
    "planningHorizon": "31.07.2026"
  }
}
```

---

##### 1) Выявление проблем по зависимостям

Backend анализирует список `Зависимости проектов`.

Для каждой зависимости:

```json
{
  "mainProject": "P2",
  "dependentProject": "P1"
}
```

проверяется:
- существует ли основной проект;
- существует ли зависимый проект;
- есть ли у основного проекта отклонения;
- завершен ли основной проект;
- не начинается ли зависимый проект раньше завершения основного.

**Правила формирования проблем:**
1. Зависимый проект зависит от проекта с отклонением
```json
{
  "type": "dependency_deviation",
  "source": "dependency_analysis",
  "mainProject": "P2",
  "dependentProject": ["P1"],
  "reason": "Зависимый проект связан с проектом, у которого есть текущее отклонение"
}
```

2. Зависимый проект начинается до завершения основного (dependentProject.startDate < mainProject.endDate)
```json
{
  "type": "dependency_date_conflict",
  "source": "dependency_analysis",
  "mainProject": "P2",
  "dependentProject": ["P1"],
  "reason": "Зависимый проект начинается раньше завершения основного проекта"
}
```

---

##### 2) Выявление проблем по срокам

Backend сравнивает даты проектов с:
- `Горизонт планирования`
- `Критический срок`

**Правила формирования проблем:**
1. Проект выходит за горизонт планирования (mainProject.endDate > planningHorizon)
```json
{
  "type": "planning_horizon_violation",
  "source": "timeline_analysis",
  "mainProject": "P3",
  "reason": "Дата окончания проекта выходит за горизонт планирования",
  "facts": {
    "projectEndDate": "20.08.2026",
    "planningHorizon": "31.07.2026"
  }
}
```

2. Проект выходит за критический срок (mainProject.endDate > criticalDeadline)
```json
{
  "type": "critical_deadline_violation",
  "source": "timeline_analysis",
  "mainProject": "P3",
  "reason": "Дата окончания проекта выходит за критический срок портфеля",
  "facts": {
    "projectEndDate": "20.08.2026",
    "criticalDeadline": "31.07.2026"
  }
}
```

3. Узкое место по срокам

Если проект:
- находится в длинной цепочке зависимостей;
- имеет отклонение;
- влияет на несколько зависимых проектов;

```json
{
  "type": "timeline_bottleneck",
  "source": "timeline_analysis",
  "mainProject": "P2",
  "dependentProject": ["P3", "P4"],
  "reason": "Проект является узким местом по срокам"
}
```

---

##### 3) Выявление проблем по ресурсам

Backend суммирует трудоемкость проектов по ролям:
`Суммарная потребность = сумма трудоемкости всех проектов по роли`

Для каждой роли рассчитывается Дефицит(Профицит): `лимит - суммарная потребность`. Например: 
```json
{
  "role": "Разработчики",
  "required": 1200,
  "limit": 900,
  "delta": -300
}
```
**Правила формирования проблем:**
1. Превышение лимита ресурсов (required > limit)
```json
{
  "type": "resource_deficit",
  "source": "resource_analysis",
  "role": "Разработчики",
  "reason": "Суммарная потребность превышает доступный лимит ресурсов",
  "facts": {
    "required": 1200,
    "limit": 900,
    "deficit": 300
  }
}
```

---

##### 4) Выявление проблем по текущим отклонениям

Backend просматривает поле `Текущие отклонения` у каждого проекта.

Если поле не пустое, происходит поиск зависимых проектов и формируется проблема:
```json
{
  "type": "project_deviation",
  "source": "deviation_analysis",
  "mainProject": "P1",
  "reason": "У проекта существует текущее отклонение",
  "facts": {
    "deviation": "Отставание на 10 дней"
  },
  "propagation": {
    "hasPropagation": true,
    "dependentProject": ["P3", "P4"]
  }
}
```

---

#### Определение уровня влияния риска
После формирования проблемы backend определяет уровень влияния будущего риска.

Используются три уровня:
1. Низкий
2. Средний
3. Высокий

**Правила определения влияния**

**1. Низкий уровень**

Назначается, если:
- проблема относится к одному проекту;
- нет зависимых проектов;
- нет превышения критического срока;
- нет дефицита ресурсов.

**2. Средний уровень**

Назначается, если:
- проблема влияет на один зависимый проект;
- есть отклонение у проекта в работе;
- есть локальное превышение срока;
- есть умеренный дефицит ресурсов (до 20%). 

**3. Высокий уровень**

Назначается, если:
- проект выходит за критический срок;
- отклонение распространяется на несколько зависимых проектов;
- дефицит ресурсов значительный (> 20%);
- проблема ставит под угрозу реализуемость портфеля.

Пример результата:
```json
{
  "problemId": "problem-001",
  "type": "dependency_deviation",
  "impact": "Высокий",
  "impactReason": "Отклонение проекта P1 распространяется на зависимые проекты P2 и P3"
}
```

---

#### Преобразование проблемы в черновик риска
backend преобразует аналитическую проблему в черновой объект риска.

Черновик риска уже соответствует доменному понятию Риск, но его текстовая формулировка должна быть уточнена ИИ.

```json
{
  "riskId": "risk-001",
  "risk": "",
  "impact": "Высокий",
\  "sourceProblem": {
    "type": "dependency_deviation",
    "mainProject": "P2",
    "dependentProject": ["P1", "P3"],
    "reason": "Зависимый проект связан с проектом, у которого есть текущее отклонение", 
    "facts": {
      "deviation": "Отставание на 10 дней", 
      "constraints": "Нельзя переносить релиз"
    }
  }
}
```

---

#### Уточнение формулировки риска с помощью ИИ
ИИ используется только после того, как backend уже определил:
- тип проблемы;
- источник проблемы;
- проект или роль;
- связанные проекты;
- факты;
- уровень влияния.

ИИ не должен изменять расчетные факты и уровень влияния.

Вход для ИИ:
```json
{
  "riskId": "risk-001",
  "risk": "",
  "impact": "Высокий",
  "problem": {
    "type": "dependency_deviation",
    "mainProject": "P2",
    "dependentProject": ["P1", "P3"],
    "reason": "Зависимый проект связан с проектом, у которого есть текущее отклонение", 
    "facts": {
      "deviation": "Отставание на 10 дней", 
      "constraints": "Нельзя переносить релиз"
    }
  }
}
```

**Промпт:** 

```
Ты формируешь текст риска для системы стратегического планирования мультипроекта.

Используй только переданные данные.
Не добавляй новые проекты, роли, сроки, ограничения или причины.
Не изменяй уровень влияния риска.
Формулировка должна:
- быть краткой;
- начинаться со слова "Риск";
- описывать возможное последствие и его причину;
- быть пригодной для отображения на фронтенде.

Входные данные:

```json
{
  "riskId": "risk-001",
  "risk": "",
  "impact": "Высокий",
  "problem": {
    "type": "dependency_deviation",
    "mainProject": "P2",
    "dependentProject": ["P1", "P3"],
    "reason": "Зависимый проект связан с проектом, у которого есть текущее отклонение",
    "facts": {
      "deviation": "Отставание на 10 дней",
      "constraints": "Нельзя переносить релиз"
    }
  }
}

Верни только JSON:

{
  "risk": "..."
}
```

---

#### Валидация ответа ИИ
Backend проверяет ответ ИИ перед сохранением.

Проверки:
1. Ответ является валидным JSON.
2. Поле risk не пустое.
3. В тексте риска нет проектов, которых нет в исходных данных.
4. В тексте риска нет ролей, которых нет в исходных данных.
5. ИИ не добавил новых фактов.

Если ответ ИИ не прошел проверку:
- возвращается ошибка "Некорректный ответ ИИ".

#### Формирование готового объекта риска
После валидации backend формирует итоговый объект риска.

Итоговый формат соответствует термину Риск:

```json
{
  "id": "risk-001",
  "risk": "Риск задержки проекта P2 из-за отклонения проекта P1",
  "impact": "Высокий",
}
```

Для внутреннего хранения дополнительно сохраняются технические поля:

```json
{
  "riskId": "risk-001",
  "risk": "Риск задержки проекта P2 из-за отклонения проекта P1",
  "impact": "Высокий",
  "problem": {
    "type": "dependency_deviation",
    "mainProject": "P2",
    "dependentProject": ["P1", "P3"],
    "reason": "Зависимый проект связан с проектом, у которого есть текущее отклонение",
    "facts": {
      "deviation": "Отставание на 10 дней",
      "constraints": "Нельзя переносить релиз"
    }
  }
}
```

---

#### Возврат списка рисков
После обработки всех проблем backend возвращает список рисков в составе результата анализа:
```json
{
  "risks": [
    {
      "id": "risk-001",
      "risk": "Риск задержки проекта P2 из-за отклонения проекта P1",
      "impact": "Высокий",
    },
    {
      "id": "risk-002",
      "risk": "Риск дефицита ресурсов по роли Разработчики",
      "impact": "Средний",
    }
  ]
}
```

На фронтенде эти данные отображаются в блоке `Выявленные риски`.

---

## 3. Анализ реализуемости портфеля по срокам
**Определение:** это backend-процесс, который определяет, может ли портфель проектов быть завершен в рамках заданного горизонта планирования и критического срока с учетом календарных параметров проектов и межпроектных зависимостей.

Процесс направлен на выявление:
- проектов, выходящих за горизонт планирования;
- проектов, нарушающих критический срок;
- конфликтов в зависимостях между проектами;
- каскадных нарушений сроков.

**Входные данные:**
- `Горизонт планирования`
```json
{
  "planningHorizon": "2023-12-31"
}
```
- `Критический срок`
```json
{
  "criticalDate": "2024-01-01"
}
```
- `Реестр проектов`
```json
[
  {
    "projectId": "P1",
    "name": "Модуль авторизации",
    "status": "В работе",
    "startDate": "2026-03-01",
    "endDate": "2026-04-30",
    "deviations": "Отставание на 10 дней"
  }
]
```
- `Список зависимостей`
```json
[
  {
    "mainProject": "P1",
    "dependentProject": "P2",
    "type": "finish_to_start"
  }
]
```

**Логика формирования:**
1. Валидация входных данных

Проверяется:
- наличие горизонта планирования;
- наличие критического срока;
- наличие хотя бы одного проекта;
- наличие дат начала и окончания у каждого проекта

Если найдены критические ошибки, дальнейший анализ не выполняется.

2. Проверка корректности дат

Проверяется:

- дата окончания не раньше даты начала;
- даты имеют корректный формат.

3. Проверка горизонта планирования

```python
if project.endDate > planningHorizon:
    violations.append({
        "type": "planning_horizon_violation",
        "projectId": project.id,
        "projectName": project.name,
        "projectEndDate": project.endDate,
        "planningHorizon": planningHorizon
    })
```

4. Проверка критического срока

```python
if project.endDate > criticalDate:
    violations.append({
        "type": "critical_deadline_violation",
        "projectId": project.id,
        "projectName": project.name,
        "projectEndDate": project.endDate,
        "criticalDate": criticalDate
    })
```

5. Проверка зависимостей

Все зависимости интерпретируются как Finish-to-Start. 
Если зависимый проект начинается раньше завершения основного → ошибка. 
При выявлении нарушения зависимости выполняется проверка каскадного влияния на связанные проекты.

Проверка прямых конфликтов зависимотей:

```python
if dependentProject.startDate < mainProject.endDate:
    violations.append({
        "type": "dependency_schedule_conflict",
        "mainProject": mainProject.id,
        "dependentProject": dependentProject.id,
        "mainProjectEndDate": mainProject.endDate,
        "dependentProjectStartDate": dependentProject.startDate
    })
```

6. Анализ каскадного влияния задержек.
Если у основного проекта есть отклонение по срокам, backend проверяет, повлияет ли оно на зависимые проекты.

Например:
```
P1 отстает на 10 дней
P2 зависит от P1
P3 зависит от P2
```
Тогда задержка может распространиться по цепочке:
`P1 → P2 → P3`

**ИИ-интерпретация:**

Промпт:
``` 
Ты выполняешь анализ реализуемости портфеля по срокам.

Твоя задача:
- проанализировать уже выявленные backend-нарушения;
- проанализировать текстовые отклонения проектов;
- определить возможное каскадное влияние отклонений на зависимые проекты;
- сформировать краткие текстовые выводы для отображения пользователю.

Важно:
- не изменяй и не пересчитывай backend-нарушения;
- не придумывай новые проекты или зависимости;
- используй только переданные данные;
- допускается логический анализ текстовых отклонений;
- допускается определение возможной передачи задержек по цепочке зависимостей;
- выводы должны быть краткими, понятными и ориентированными на пользователя интерфейса.

---

# Входные данные

{
  "planningHorizon": "2026-07-31",
  "criticalDate": "2026-07-31",

  "projects": [
    {
      "projectId": "P1",
      "projectName": "Модуль авторизации",
      "status": "В работе",
      "startDate": "2026-03-01",
      "endDate": "2026-04-30",
      "deviations": "Отставание на 10 дней"
    }
  ],

  "dependencies": [
    {
      "mainProject": "P1",
      "dependentProject": "P2",
      "type": "finish_to_start"
    }
  ],

  "violations": [
    {
      "type": "planning_horizon_violation",
      "projectId": "P3",
      "projectName": "Модуль аналитики",
      "projectEndDate": "2026-08-20",
      "planningHorizon": "2026-07-31"
    },
    {
      "type": "critical_deadline_violation",
      "projectId": "P3",
      "projectName": "Модуль аналитики",
      "projectEndDate": "2026-08-20",
      "criticalDate": "2026-07-31"
    },
    {
      "type": "dependency_schedule_conflict",
      "mainProject": "P1",
      "dependentProject": "P2",
      "mainProjectEndDate": "2026-04-30",
      "dependentProjectStartDate": "2026-04-15"
    }
  ]
}

Проанализируй:
- проекты, выходящие за горизонт планирования;
- проекты, нарушающие критический срок;
- конфликты зависимостей;
- возможное распространение задержек;
- цепочки зависимых проектов с высоким риском.
Если в текстовом поле deviations указаны:
- отставание;
- задержка;
- перенос;
- превышение сроков;
то допускается вывод о возможном каскадном влиянии на зависимые проекты.
Не используй сложные объяснения.
Выводы должны быть короткими и подходить для отображения в интерфейсе.

Верни результат строго в JSON-формате:

{
  "summary": [
    "Горизонт планирования покрывает портфель частично: часть проектов укладывается в целевой период, но проект E выходит за критический срок.",
    "Проекты, выходящие за критические сроки: C и E.",
    "Наибольшее влияние на сроки создают зависимости B → D и C → E → F.",
    "Проект C уже имеет отклонение, которое влияет на последующий проект E."
  ],

  "attentionProjects": [
    {
      "projectId": "P3",
      "projectName": "Модуль аналитики",
      "reason": "Выходит за критический срок"
    }
  ],

  "cascadeRisks": [
    {
      "sourceProject": "P1",
      "affectedProject": "P2",
      "reason": "Отставание основного проекта может сместить начало зависимого проекта"
    }
  ]
}
```

## 4. Анализ загрузки и дефицита ресурсов
**Определение:** Сравнение потребности ресурсов с лимитами

**Входные данные:**
- проекты (трудоемкость по ролям)
- лимиты

**Алгоритм:**
1. Суммирование:
`total_dev = sum(project.dev for project in projects)` 
2. Сравнение:
`delta_dev = total_dev - limit_dev`

**Формат результата:**
```json
{
  "resources": [
    {
      "role": "Разработчики",
      "required": 1200,
      "limit": 1000,
      "delta": -200
    }
  ]
}
```
**Особенности:**
- отрицательное значение `delta` → дефицит
- положительное `delta` → профицит

## 5. Анализ текущих отклонений
**Определение:** Анализ влияния существуюзих отклонений проектов на портфель

**Входные данные:**
- Текущие отклонения
- зависимости

**Алгоритм:**
1. Извлечение отклонений из проектов
2. Определение влияния:
    - если проект участвует в цепочке → влияние усиливается (среднее и выше)
    - если в формулировке указана степень отклонения (сильное, небольшое и т.д.) → влияние устанавливается исходя из этого
3. ИИ

Промпт:
```
Ты выполняешь анализ текущих отклонений проектов в системе стратегического планирования мультипроекта.

Твоя задача:
- проанализировать существующие отклонения проектов;
- определить степень опасности отклонения для портфеля;
- определить возможное распространение влияния по зависимостям;
- сформировать краткое описание для отображения пользователю.

Важно:
- не изменяй и не придумывай проекты;
- используй только переданные данные;
- не рассчитывай сроки и ресурсы заново;
- анализируй только уже существующие отклонения;
- если проект участвует в цепочке зависимостей, влияние может усиливаться;
- если в тексте отклонения явно указана степень ("сильное", "критическое", "небольшое" и т.д.), учитывай ее при определении опасности;
- ответ должен быть кратким и структурированным.

---

# Входные данные

{
  "projects": [
    {
      "projectId": "P1",
      "projectName": "Модуль авторизации",
      "status": "В работе",
      "deviation": "Сильное отставание по разработке",
    }
  ],

  "dependencies": [
    {
      "mainProject": "P1",
      "dependentProject": "P2"
    }
  ]
}

---

# Что нужно определить

Для каждого проекта с отклонением:

1. Степень опасности для портфеля:
- Низкая
- Средняя
- Высокая

2. Возможность передачи влияния:
- есть ли влияние на зависимые проекты;
- на какие проекты влияние может распространиться.

3. Краткое описание влияния отклонения.

---

# Правила анализа

## Низкая опасность
Если:
- отклонение небольшое;
- проект не участвует в цепочке зависимостей;
- влияние локальное.

## Средняя опасность
Если:
- проект участвует в цепочке зависимостей;
- отклонение может повлиять на следующий проект;
- отклонение умеренное.

## Высокая опасность
Если:
- отклонение сильное или критическое;
- проект влияет на несколько зависимых проектов;
- влияние может распространиться на значительную часть портфеля.

---

# Формат ответа

Верни JSON:

{
  "deviations": [
    {
      "project": "Название проекта",
      "deviation": "Описание отклонения",
      "portfolioImpact": "Низкая | Средняя | Высокая",
      "dependencyPropagation": "Описание распространения влияния"
    }
  ]
}

---

# Пример ответа

{
  "deviations": [
    {
      "project": "Модуль авторизации",
      "deviation": "Сильное отставание по разработке",
      "portfolioImpact": "Высокая",
      "dependencyPropagation": "Отклонение может повлиять на проект Платежная система"
    }
  ]
}
```

## 6. ИИ-объяснения
**Определение:** Человекочитаемое описание состояния портфеля

**Входные данные:**
ИИ получает агрегированные результаты аналитики, сформированные backend-модулем:

1. Граф зависимостей проектов
2. Список выявленных рисков
3. Анализ реализуемости портфеля по срокам
4. Анализ загрузки и дефицита ресурсов
5. Анализ текущих отклонений

Промпт:
```
Ты формируешь ИИ-объяснение текущего состояния портфеля проектов.

Твоя задача:
- кратко описать текущее состояние портфеля;
- объяснить ключевые риски;
- объяснить проблемы сроков;
- объяснить проблемы ресурсов;
- объяснить влияние текущих отклонений.

Важно:
- используй только переданные данные;
- не придумывай новые проекты, сроки, риски или зависимости;
- не изменяй рассчитанные уровни влияния;
- не пересчитывай аналитику;
- объяснение должно быть кратким, понятным и ориентированным на пользователя интерфейса;
- не используй вводные фразы;
- не используй списки;
- ответ должен быть цельным текстом.

---

# Входные данные

{
  "dependencyGraph": {dependencyGraph},

  "risks": {risks},

  "timelineAnalysis": {timelineAnalysis},

  "resourceAnalysis": {resourceAnalysis},

  "deviationAnalysis": {deviationAnalysis}
}

---

# Формат ответа

Верни JSON:

{
  "summary": "Текст ИИ-объяснения"
}
```

## 7. ИИ-рекомендации
**Определение:** Рекомендации по улучшению состояния портфеля

**Входные данные:**
ИИ-рекомендации формируются на основе уже рассчитанных backend-аналитикой данных.

**Используемые данные:**
1. `Граф зависимостей проектов`
   - проекты;
   - связи между проектами;
   - цепочки зависимостей.
2. `Список выявленных рисков`
   - формулировка риска;
   - степень влияния.
3. `Анализ реализуемости портфеля по срокам`
   - проекты, выходящие за горизонт планирования;
   - проекты, выходящие за критический срок;
   - узкие места по срокам.
4. `Анализ загрузки и дефицита ресурсов по ролям`
   - роль;
   - суммарная потребность;
   - лимит;
   - дефицит / профицит.
5. `Анализ текущих отклонений`
   - проект;
   - отклонение;
   - опасность для портфеля;
   - передача по зависимостям.
6. `Портфельные ограничения`
   - лимит аналитиков;
   - лимит разработчиков;
   - лимит тестировщиков;
   - критический срок.

Промпт:
```
Ты формируешь ИИ-рекомендации для этапа анализа в системе стратегического планирования мультипроекта.

Твоя задача:
- проанализировать предоставленные данные;
- сформировать практические рекомендации для пользователя;
- предложить действия по снижению рисков, устранению дефицитов ресурсов, корректировке сроков и контролю отклонений.

Важно:
- не пересчитывай сроки, ресурсы и риски;
- не изменяй уровни влияния рисков;
- не придумывай новые проекты, роли, зависимости, сроки или ограничения;
- используй только переданные входные данные;
- рекомендации должны быть конкретными и применимыми;
- не формулируй общие советы без связи с данными;
- если рекомендация связана с конкретным проектом, укажи этот проект;
- если рекомендация связана с ресурсами, укажи роль;
- если рекомендация связана с зависимостью, укажи основной и зависимый проект.

---

# Входные данные

{
  "dependencyGraph": {dependencyGraph},

  "risks": {risks},

  "timelineAnalysis": {timelineAnalysis},

  "resourceAnalysis": {resourceAnalysis},

  "deviationAnalysis": {deviationAnalysis},

  "portfolioConstraints": {portfolioConstraints}
}

---

# Требуемый формат ответа

Верни только JSON без дополнительного текста:

{
  "recommendations": [
    {
      "title": "Краткий заголовок рекомендации",
      "text": "Конкретное действие, которое следует выполнить",
      "basis": "На основании каких данных сформирована рекомендация"
    }
  ]
}

---

# Пример ответа

{
  "recommendations": [
    {
      "title": "Перераспределить ресурсы разработчиков",
      "text": "Следует пересмотреть распределение разработчиков между проектами P2 и P3, так как по роли Разработчики выявлен дефицит 200 чел.часов.",
      "basis": "resourceAnalysis.resources: Разработчики, delta = -200"
    },
    {
      "title": "Усилить контроль проекта P2",
      "text": "Проект P2 имеет отклонение и влияет на зависимый проект P3, поэтому его необходимо вынести в зону приоритетного контроля.",
      "basis": "deviationAnalysis.deviations: P2, portfolioImpact = Высокая"
    }
  ]
}
```
